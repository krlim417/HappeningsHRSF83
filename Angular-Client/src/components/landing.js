angular.module('happeningsApp')

  .controller('landingController', () => {

  })
  .component('landing', {
    controller: 'landingController',
    templateUrl: '../../templates/landing.html',
  });
